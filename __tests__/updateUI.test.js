import { updateUI } from "../src/client/js/updateUI"

// Utilizei como ajuda o código descrito pelo mentor no link abaixo:

// https://knowledge.udacity.com/questions/253741#253791

describe("Testing the page update based on api data", () => {
    test("Testing the updateUI() function", () => {
        expect(updateUI).toBeDefined();
})});